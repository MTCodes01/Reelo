version: '3.8'

services:
  yt-downloader:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "3294:7654"
    volumes:
      - ./downloads:/app/downloads
    environment:
      - HOST=0.0.0.0
      - PORT=7654
      - LOG_LEVEL=info
    restart: unless-stopped
